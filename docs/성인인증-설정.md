# 성인 게시판 본인확인 — 설정 순서

코드는 이미 다 들어가 있습니다. **아래 순서대로 하면 켜집니다.**
현재 상태: `ADULT_BOARD_ENABLED=false` 라서 성인 게시판 자체가 보이지 않습니다(안전).

---

## 1단계. 사업자등록 (본인이 직접)

본인확인 서비스는 **사업자만 계약할 수 있습니다.**

- 국세청 홈택스 또는 정부24에서 **개인사업자** 등록 (수수료 없음)
- 업종: 정보통신업 / 포털 및 기타 인터넷 정보매개 서비스업 (`63120`) 정도가 무난
- 통신판매업 신고는 **불필요** (본인인증만 쓰고 결제를 받지 않는 경우)

> 사업자등록을 하면 종합소득세 신고 의무가 생깁니다. 수익이 없어도 무실적 신고는 해야 합니다.

---

## 2단계. 포트원 채널 만들기 — **완료 (2026-08-13)**

> 처음엔 KG이니시스 통합인증으로 잡았으나, 실제 계약은 **NHN KCP 휴대폰 본인확인**으로 갔습니다.
> 포트원이 KCP의 신규 연동방식(PG Provider `kcp_v2`)을 감싸 주므로 **코드는 그대로 씁니다.**

1. https://admin.portone.io → **결제 연동 → 연동 정보 → 채널 관리 → 채널 추가**
2. 결제대행사 **NHN KCP**, 채널 속성 **본인인증**
3. **PG상점아이디(사이트코드)** = KCP 사이트코드 (`PO02S`)
4. **본인인증 인증키** = KCP 사이트키(`site_key`) — KCP에서 받아 콘솔에만 입력

⚠️ `PG-API 인증서`·`PG-API 개인 키`는 **결제용이라 비워 두면 됩니다**(필수 표시 없음).
   인증서가 없어 저장이 안 되는 것처럼 보이면 채널 속성이 `결제`로 되어 있는지 확인하세요.

⚠️ KCP 상점관리자의 **부가서비스 → 휴대폰본인확인 → 인증결과URL설정**에
   `checkout-service.prod.iamport.co`가 있어야 합니다.
   포트원 경유 계약이면 **기본 설정 URL에 자동 등록**돼 있습니다(회색·수정 불가). 확인만 하세요.
   ⚠️ `설정함 → 설정하지 않음`으로 바꿔 저장하면 **등록된 URL이 전부 삭제**됩니다.

---

## 2.5단계. DB에 2차 보완 SQL 실행

`docs/sql/adult-verification.sql` 을 이미 실행하셨다면, **`docs/sql/adult-verification-2.sql` 도 실행**하세요.

- CI 대신 **DI**를 쓰도록 컬럼 이름을 바꿉니다(`adult_ci_hash` → `adult_di_hash`)
- 한 번 쓴 본인확인 번호를 재사용하지 못하게 막습니다

> **CI는 신청하지 마세요.** KCP의 CI·DI 제공신청서에서 **DI만 체크**하면 됩니다.
> CI는 모든 기관 공통 식별자라 연계정보 안전조치 실태점검 대상이 될 수 있는데,
> 우리 용도(한 사람당 한 계정)는 사이트별 값인 DI로 충분합니다.

---

## 3단계. 키 3개 넣기

### (a) 프론트 — `public/agegate.js` — **입력 완료 (2026-08-13)**

> ⚠️ 파일이 바뀌었습니다. 광고 심사 대응으로 게이트 코드를 `palo.js`에서 분리했습니다.
> 이제 `public/agegate.js`에 있고, 필요할 때만 브라우저가 받아옵니다.

```js
var PORTONE_STORE_ID = "store-73603042-1b6d-4eb5-8063-84ed34f1242e";
var PORTONE_CHANNEL_KEY = "channel-key-9ae37405-3046-4646-8436-b28e55ab7a7b";
```

두 값이 채워지면 `READY`가 자동으로 `true`가 됩니다.
(브라우저에 노출돼도 되는 **공개 값**입니다. 비밀 값은 아래 서버 환경변수 쪽입니다.)

### (b) 서버 — Vercel 환경변수 (Settings → Environment Variables)

| 이름 | 값 |
|---|---|
| `PORTONE_API_SECRET` | 관리자 콘솔 → **API Secret** (⚠️ 절대 공개 금지) |
| `ADULT_ID_SALT` | 아무도 모르는 긴 무작위 문자열 |

`ADULT_ID_SALT`는 아래로 만드세요:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> ⚠️ **`ADULT_ID_SALT`는 한 번 정하면 절대 바꾸지 마세요.** 바꾸면 기존 인증자들의
> 중복 확인 값이 전부 달라져서, 한 사람이 계정을 여러 개 인증할 수 있게 됩니다.
>
> 예전 이름 `ADULT_CI_SALT`로 이미 넣어 두셨다면 그대로 둬도 동작합니다(코드가 둘 다 받습니다).
> 이름만 바꾸실 거면 **값은 그대로** 옮기세요.

환경변수를 넣은 뒤 **Redeploy** 해야 반영됩니다.

---

## 4단계. DB 설정 실행

Supabase → SQL Editor 에서 `docs/sql/adult-verification.sql` 전체를 붙여넣고 실행.

여러 번 실행해도 안전합니다.

---

## 5단계. 게시판 켜기

`public/palo.js` (스위치는 여전히 palo.js에 있습니다):

```js
var ADULT_BOARD_ENABLED=true;
```

⚠️ **네이버 로그인 검수가 끝난 뒤에 켜세요.** 제출한 소명서에 "성인 콘텐츠 없음"으로
적었기 때문에, 검수 중에 켜면 소명 내용과 달라집니다.

---

## 6단계. 문서 갱신 (법적 의무)

- **이용약관** — 성인 게시판 이용 조건, 금지 콘텐츠, 제재 규정
- **개인정보처리방침** — 본인확인 항목 추가:
  - 수집 항목: 중복가입확인정보(CI)의 암호화 값, 인증 일시
  - 수집 목적: 청소년보호법에 따른 연령 확인
  - 보유 기간: 회원 탈퇴 시까지
  - 수탁자: (주)케이지이니시스 — 본인확인 업무

---

## 동작 확인 체크리스트

켠 뒤 이것들을 직접 확인하세요:

- [ ] 로그아웃 상태에서 성인 게시판 클릭 → 로그인 창
- [ ] 로그인·미인증 상태에서 클릭 → 인증 안내 창
- [ ] 인증 완료 → 게시판 진입 가능
- [ ] 다른 계정으로 같은 명의 인증 시도 → "이미 다른 계정에서 인증됨" 거부
- [ ] 미인증 계정에서 브라우저 콘솔로 확인 (아래가 **거부**되어야 정상)

```js
await supabase.from('profiles').update({adult_verified:true}).eq('id', AUTH.user.id)
// → "성인 인증 정보는 직접 변경할 수 없습니다" 오류가 나야 함

await supabase.from('posts').select('*').eq('board','adult')
// → 0건이어야 함
```

---

## 구조 요약

```
브라우저 ──(1) 인증창 요청──> 포트원 SDK ──> PASS/토스/카카오…
    │                                            │
    │<──────────(2) identityVerificationId ───────┘
    │
    └─(3) POST /api/auth/adult-verify ─> 서버
                                          │
                                          ├─(4) 포트원 API로 결과 재조회 ★
                                          ├─(5) 연 나이 19세 이상 판정
                                          ├─(6) CI 해시로 중복 계정 확인
                                          └─(7) profiles.adult_verified = true
```

**★ (4)가 핵심입니다.** 브라우저가 보낸 값을 믿지 않고 서버가 포트원에 직접 물어봅니다.
이게 없으면 콘솔에서 가짜 id를 보내는 것만으로 뚫립니다.

### 저장하는 것 / 안 하는 것

| 저장함 | 저장 안 함 |
|---|---|
| `adult_verified` (예/아니오) | 이름 |
| `adult_verified_at` (일시) | 생년월일 |
| `adult_di_hash` (해시값) | 휴대폰번호 |
| | 원본 CI |

생년월일은 나이 계산에만 쓰고 즉시 버립니다. 해시값은 유출돼도 개인을 특정할 수 없습니다.

---

## 비용 예상

인증은 **계정당 평생 1회**입니다.

| 인증 회원 수 | 총 비용(VAT 포함) |
|---|---|
| 100명 | 4,400원 |
| 500명 | 22,000원 |
| 1,000명 | 44,000원 |
| 3,000명 | 132,000원 |

월 고정비는 없습니다. 쓴 만큼만 청구됩니다.
